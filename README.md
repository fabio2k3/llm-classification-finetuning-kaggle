# llm-classification-finetuning-kaggle
Predict user preference between two LLM responses using Chatbot Arena data. The model must mitigate biases (position, verbosity, self-promotion) to act as a reward model in RLHF, improving alignment with real human preferences.
